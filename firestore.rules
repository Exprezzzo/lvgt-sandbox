rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    match /categories/{docId} {
      allow read: if true;
    }
    match /vendors/{docId} {
      allow read: if true;
    }
  }
}
